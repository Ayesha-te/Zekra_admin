# Zekra Sweets Admin Panel

Standalone React admin app for managing products through the Zekra Sweets API.

## Local Run

```bash
npm install
cp .env.example .env
npm run dev
```

Set `VITE_API_URL` to the backend origin. For local backend development, use:

```env
VITE_API_URL=http://localhost:4000
```
